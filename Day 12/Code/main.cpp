#include <iostream>
using namespace std;
int main () {
    // Bitwise AND (&),Bitwise OR (|), Bitwise XOR (^), 
    int x = 3, y = 6;
    // cout << (x & y) << endl;    // 2
    // cout << (x | y) << endl;    // 7
    // cout << (x ^ y) << endl;    // 5
    
    // Left Shift (val << shiftCount)
    // cout << (x << 2) << endl;
    // cout << (y << 2) << endl;
    
    // Right Shift (val >> shiftCount)
    // cout << (x >> 2) << endl;
    // cout << (y >> 2) << endl;
    

    // Bitwise Negation/NOT (~),
    // int z = 5;
    // cout << (~z) << endl;
    // 0000000......001
    // 111111.......110

    return 0;
}